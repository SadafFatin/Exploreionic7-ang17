import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alpharecon.mobile',
  appName:'SecuRecon',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
